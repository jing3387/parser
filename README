# LL(1) parser

## Installing
If the files ``parser'' and ``test'' aren't executable:
    chmod +x parser test

## Usage
To use the parser:
    ./parser file

To test the parser with the tests in t/:
    ./test
